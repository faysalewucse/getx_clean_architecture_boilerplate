import 'package:flutter/cupertino.dart';

class AppPadding {
  static const screenPaddingAll = EdgeInsets.all(16.0);
  static const screenPaddingHV =
      EdgeInsets.symmetric(horizontal: 20.0, vertical: 16.0);

  //-------you can add/edit as your needs--------//
  static const allPadding10 = EdgeInsets.all(10.0);

  //-------you can add/edit as your needs--------//
  static const paddingLRB = EdgeInsets.only(left: 8.0, right: 8.0, bottom: 8.0);
  static const paddingLRT = EdgeInsets.only(left: 8.0, right: 8.0, top: 8.0);

}
